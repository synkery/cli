# Synkery

Early crates.io placeholder for the Synkery CLI.

This crate reserves the `synkery` package name for `cargo install synkery` while
the first usable CLI release is being prepared in the main `synkery/cli`
repository.
